// This screen is never actually rendered — the tab button in _layout.tsx
// intercepts taps and opens the Acuity membership catalog in the browser instead.
import { View } from 'react-native';
export default function MembershipsScreen() {
  return <View />;
}
