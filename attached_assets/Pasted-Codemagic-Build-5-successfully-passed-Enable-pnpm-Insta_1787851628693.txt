Codemagic Build #5 successfully passed:

Enable pnpm
Install dependencies
Expo prebuild
Install CocoaPods dependencies
Set up code signing identities

It then failed in Build iOS with these exact errors:

xcodebuild: error: The workspace named "FITCLUB15" does not contain a scheme named "fitclub15".

xcodebuild: error: option '-exportOptionsPlist' requires an argument

The current workflow manually archives/exports with xcodebuild and hardcodes:

-workspace fitclub15.xcworkspace

-scheme fitclub15

and:

-exportOptionsPlist $CM_EXPORT_OPTIONS_PATH

Do not make a build, push, publish, TestFlight submission, signing change, dependency change, Expo package change, or application-code change.

First perform a read-only/static investigation of the project configuration and existing generated/native naming information available in the repository to determine the expected iOS workspace/project name and scheme.

Also review the current Build iOS section of codemagic.yaml.

Codemagic's supported signed-build pattern is:

xcode-project use-profiles

followed by:

xcode-project build-ipa --workspace <workspace> --scheme <scheme>

Propose the smallest safe replacement of the current manual xcodebuild archive + xcodebuild -exportArchive block with Codemagic's xcode-project build-ipa command.

Do not guess the scheme name.

If the exact scheme cannot be proven from existing repository/configuration files, report that fact and propose a temporary diagnostic command for the next Codemagic run that lists the generated workspace schemes after Expo prebuild, without attempting an archive.

Do not edit anything yet.

Report:

the currently hardcoded workspace and scheme,
what evidence exists for the actual generated workspace/project name,
whether the exact scheme can be proven,
whether $CM_EXPORT_OPTIONS_PATH is defined anywhere in the workflow,
the exact minimal codemagic.yaml change you recommend,
whether that change would still leave publishing: and submit_to_testflight: true absent.

Stop after reporting. Do not modify files.